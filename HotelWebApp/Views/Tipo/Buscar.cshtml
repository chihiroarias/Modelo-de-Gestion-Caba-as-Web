@model HotelMVC.Models.EntidadesModel.TipoModel

@{
    ViewData["Title"] = "Buscar";
}

<h1>Buscar</h1>

<h4>Tipo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Buscar" method="get">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NombreInput" class="control-label"></label>
                <input asp-for="NombreInput" class="form-control" />
                <span asp-validation-for="NombreInput" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Buscar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


<div>
    <a asp-action="Index">Back to List</a>
</div>
<div>
    @ViewBag.Message
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
