@{
    ViewData["Title"] = "Home Page";
}

<h3>Buscar por monto y codigo del tipo</h3>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Buscar">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label for="fechafin" class="control-label">Id del Tipo</label>
                <input type="number" name="idTipo" class="form-control">
            </div>
            <div class="form-group">
                <label for="fechafin" class="control-label">Monto</label>
                <input type="number" name="monto" class="form-control">
            </div>
            <div class="form-group">
                <input type="submit" value="Buscar" class="btn btn-primary" formaction="/Cabania/Buscar" />
            </div>
        </form>
    </div>
</div>